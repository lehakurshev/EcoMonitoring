name: Deploy EcoMonitoring

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
          ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} << 'EOF'
            set -e

            echo "🔍 Проверка Docker..."
            # ... (остальной код установки Docker и docker-compose остаётся без изменений)

            mkdir -p ${{ secrets.DEPLOY_PATH }}
            cd ${{ secrets.DEPLOY_PATH }}

            if [ ! -d ".git" ]; then
              echo "📥 Клонирование репозитория..."
              git clone https://github.com/${{ github.repository }} .
            else
              echo "📦 Репозиторий существует — обновляем"
              git fetch origin
              git checkout main
              git reset --hard origin/main
              git clean -fd
            fi

            echo "Создание .env файла..."
            cat > .env << EOL
            MONGODB_PASSWORD=${{ secrets.MONGODB_PASSWORD }}
            VITE_ADMIN_PASSWORD_HASH=${{ secrets.VITE_ADMIN_PASSWORD_HASH }}
            EOL

            chmod +x deploy.sh
            ./deploy.sh ${{ secrets.DEPLOY_PATH }}

          EOF
